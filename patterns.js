// strict_translator_fr/patterns.js
// Dictionnaire FR → patterns SDF stricts (HF3)

module.exports = {
    // Répétition linéaire
    repeat_linear: [
        "repete", "repeter", "repetition",
        "fois", "multiple", "multiplie"
    ],

    // Répétition en grille
    grid: [
        "grille", "ajoure", "ajourement",
        "quadrillage", "carreaux"
    ],

    // Répétition circulaire
    circular: [
        "circulaire", "cercle", "autour",
        "rayon", "rayonnant", "radial"
    ],

    // Motif hexagonal (type nid d'abeille)
    hex: [
        "hexagonal", "hexagone", "nid", "abeille",
        "honeycomb"
    ],

    // Motif générique
    pattern: [
        "motif", "pattern", "texture", "decor"
    ]
};
